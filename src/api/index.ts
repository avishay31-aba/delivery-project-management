/**
 * API facade — Phase A stubs reading from Zustand store.
 * Phase B+ will swap implementations without changing page imports.
 */

export { projectsApi } from '@/api/projects'
export { systemsApi } from '@/api/systems'
export { tenantsApi } from '@/api/tenants'
